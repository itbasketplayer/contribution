<script>
    $(function(){
        $('#countdown').countdown('<%= dateFormat(activity.endTime,"YYYY/MM/DD HH:mm:ss")%>')
                .on('update.countdown', function(event) {
                    var format = '%H小时%M分%S秒';
                    if(event.offset.days > 0) {
                        format = '%-d 天 ' + format;
                    }
                    if(event.offset.weeks > 0) {
                        format = '%-w 周 ' + format;
                    }
                    $(this).html(event.strftime(format));
                })
                .on('finish.countdown', function(event) {
                    $(this).parent().html('活动已结束！')
                            .parent().addClass('disabled');
                });
    })
</script>
<header>
    <div class="headerWrap">
        <div class="logo"><img src="<%= web_domain%>/images/pc/logo.jpg" /></div>
        <div class="enterPlaform"><em>572万</em>企业首选贷款平台 <a href="http://www.bing100.com/" target="_blank" class="enterButton">进入平台</a> </div>
    </div>
</header>
<div class="banner">
    <div class="countdown">距离文案征集结束还有<span id="countdown"></span></div>
    <a href="<%= web_domain%>/pc/submit?id=<%= id%>" class="bannerButton"><img src="<%= web_domain%>/images/pc/submit.png"> </a>
    <img src="<%= image_path%>/<%= activity.beginUrl%>">
</div>